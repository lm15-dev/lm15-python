"""lm15 canonical benchmark suite.

Entry point: ``python benchmarks/suite/run.py`` (run with the repo's
``.venv/bin/python``).  Emits ``benchmarks/RESULTS.json`` and regenerates
``benchmarks/BENCHMARKS.md`` from it — numbers are never hand-typed.
"""
