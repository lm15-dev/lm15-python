# Profiles & models

**`lm15.profiles.ProviderProfile` / `EndpointProfile` are deprecated (1.0.0rc2) and removed in 1.0.0**; see the migration table in [Using model profiles](../using-model-profiles.md). The compatibility policies and the request-level hatch stay.

::: lm15.profiles

::: lm15.models

## Compatibility policies

For a worked Chat Completions example, see
[Connect an unlisted OpenAI-compatible server](../connecting-openai-compatible-servers.md).
Preset strings must be recognized names; use a typed policy object for an
unlisted server's format differences.

::: lm15.compat.OpenAIChatCompat

::: lm15.compat.OpenAIResponsesCompat
